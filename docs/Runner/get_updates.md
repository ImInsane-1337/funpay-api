# Runner.get_updates()

Запрашивает список сырых событий у FunPay API.

## Сигнатура

```python
Runner.get_updates() -> dict
```

## Возвращает

`dict` — сырой ответ FunPay. Передаётся в `parse_updates()` для обработки.

← [Назад к Runner](README.md)
